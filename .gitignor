# --- Environment / Virtual Environment ---
# Môi trường ảo (venv)
venv/
.venv/
.env                 # Rất quan trọng: Không đẩy biến môi trường chứa secrets
.flaskenv

# --- Byte-compiled / Cached files ---
# File Python cache
__pycache__/
*.pyc
*.pyd
*.pyo
# Thư mục cache của ứng dụng (nếu có)
.cache/

# --- Build / Deployment ---
# Thư mục lưu trữ các file tải lên (uploads)
# Tùy thuộc vào việc bạn có muốn lưu trữ file tải lên qua Git hay không.
# Nếu bạn dùng dịch vụ lưu trữ ngoài (S3, Cloudinary), bạn nên bỏ qua thư mục này.
uploads/

# --- IDE Files (Visual Studio Code, etc.) ---
.vscode/

# --- Database Files ---
# Các file database cục bộ
*.sqlite
*.db